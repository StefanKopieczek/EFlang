task wrapper(type: Wrapper) {
    gradleVersion = '4.2.1'
}

subprojects {
    apply plugin: 'java'

      repositories {
        flatDir {
          dirs "${rootDir}/lib"
        }

        mavenCentral()
      }
}
