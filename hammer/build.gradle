apply plugin: 'application'

dependencies {
    compile project(':ef-core')
    compile project(':ear-compiler')
    compile project(':lobe-compiler')
}

mainClassName = 'hammer.Main'

run {
    args = ["${rootDir}/tests"]
}

